import styled from 'styled-components/native';
import { Animated } from 'react-native';

export const TouchableWrapper = styled(Animated.View)`
    flex: 1;
    background-color: rgba(0, 0, 0, 0.5);
`;
